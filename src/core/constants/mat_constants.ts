import { Matrix3, Matrix4 } from "three";

/**
 *  矩阵常量
 */
export class MatConstants {

    public static Mat4_IDENTITY = Object.freeze(new Matrix4());

    public static Mat3_IDENTITY = Object.freeze(new Matrix3());

}